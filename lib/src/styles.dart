import 'package:flutter/material.dart';

const invokeColor = Colors.cyanAccent;
const transferColor = Colors.yellow;
const massTransferColor = Colors.deepOrangeAccent;
const exchangeColor = Colors.deepPurpleAccent;
const burnColor = Colors.red;
const tabHeaderColor = Colors.black12;

const hoverColor = Colors.white12;

const inAssetsColor = Colors.green;
const outAssetsColor = Colors.red;

const InputDecoration inputDecoration = InputDecoration(
    // focusedBorder: focusedBorder,
    // enabledBorder: enabledBorder,
    // focusedErrorBorder: focusedErrorBorder,
    isDense: true,
    // contentPadding: EdgeInsets.only(top:6, bottom: 3, left: 8, right: 3)
);

