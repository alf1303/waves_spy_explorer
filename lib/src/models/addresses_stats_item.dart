class AddressesStatsItem {
  String address = "";
  double income = 0;
  double outcome = 0;
  String name = "";

  AddressesStatsItem.income(this.address, this.income, this.name);

  AddressesStatsItem.outcome(this.address, this.outcome, this.name);
}